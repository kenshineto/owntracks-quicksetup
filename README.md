# OwnTracks Ansible Installation

This is my fork of the ansible installer for owntracks.

- This installer will currently only run on debian trixie (13).

## Installing

```bash
git clone https://github.com/kenshineto/owntracks-quicksetup.git .
bash install.sh
```
